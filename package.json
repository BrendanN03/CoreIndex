{
  "name": "coreindex-workspace",
  "private": true,
  "description": "Run API (8010) + web (5173). Starts local /factor stub on 8000 unless COREINDEX_DEV_FACTOR_STUB=0. Override ports via COREINDEX_DEV_* env vars.",
  "scripts": {
    "dev": "node scripts/dev-stack.mjs",
    "start": "node scripts/dev-stack.mjs"
  }
}
