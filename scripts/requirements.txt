requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Optional: For JS-rendered sites (compute.exchange, silicondata.com)
# Install with: pip install playwright && playwright install chromium
playwright>=1.40.0
